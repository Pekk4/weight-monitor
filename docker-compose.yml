version: '3.8'
services:
  server:
    build: ./server
    ports:
      - "4000:4000"
    volumes:
      - ./server/data:/usr/src/app/data
    restart: unless-stopped

  client:
    build: ./client
    ports:
      - "5000:80"
    depends_on:
      - server
    restart: unless-stopped
